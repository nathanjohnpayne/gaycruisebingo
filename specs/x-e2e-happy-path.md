---
spec_id: x-e2e-happy-path
status: accepted
---

# E2E happy-path (join → Mark → BINGO → Leaderboard) + offline-Mark test against the emulator

Proves the PRD's headline metric — a full round is playable end-to-end with zero coordination beyond the app's one shared link — with a Playwright e2e suite against the Firebase Local Emulator Suite, and adds the [ADR 0006](../docs/adr/0006-offline-resilience.md) assertion that an offline Mark survives a reload (queued durably in IndexedDB) and syncs on reconnect. Runs on the Playwright harness `w0-test-harness` stood up (`playwright.config.ts`, `test:e2e`) and consumes the `firebase.json` `emulators` block that same ticket added.

## The suite

- `playwright.config.ts` — extends the `w0-test-harness` smoke config with the app `webServer` + `baseURL` wiring it always said this ticket would add: a `vite build --mode e2e` + `vite preview` of the app (see "Emulator wiring" below for why a real build), never reusing an existing listener on the dedicated e2e port so the served bundle is always the one this run just built. The Firestore/Auth/Storage emulators are NOT a `webServer` entry: `npm run test:e2e` wraps `playwright test` in `firebase emulators:exec --only auth,firestore,storage` (mirroring `test:rules`), which only starts Playwright once every emulator is ready. So `npm run test:e2e` is the emulator-backed, self-contained entry point; a bare `npx playwright test` does not boot emulators and requires them to already be running (e.g. under `npm run emulator`).
- `tests/e2e/support/env.ts` — the shared demo project id (`demo-gaycruisebingo-e2e`), a dedicated Event id (`e2e-happy-path`, never the real `med-2026`), the emulator ports `firebase.json` pins, and the dev-server URL — imported by both the config and the test support so they cannot drift apart.
- `tests/e2e/support/seed.ts` — seeds the Event + the full `ITEMS` pool (32 Prompts, `scripts/seed.mjs` — reused, not re-declared, so this suite deals boards from the exact real pool shape) into the running Firestore emulator, with security rules disabled via `@firebase/rules-unit-testing` (already a devDependency the `tests/rules/` layer relies on) — the same rules-bypassing posture `scripts/seed.mjs` gets from the Admin SDK, without adding `firebase-admin` as a dependency (it is deliberately absent from the app install; see that script's header comment).
- `tests/e2e/support/join.ts` — the zero-coordination join: land on `/` (the shared link), accept the 18+ acknowledgement, click "Continue with Google", and drive the Firebase Auth Emulator's account-chooser popup that `signInWithPopup` opens (add a new auto-generated account and submit — no real Google OAuth). Shared by both cases below so the join is asserted in exactly one place.
- `tests/e2e/support/board.ts` — selects Squares by their dealt prompt TEXT (`src/components/Board.tsx` renders no `data-testid`/role on a cell), never CSS position, so the suite survives the Board gaining more per-cell chrome (the doubt affordance, tally badges) around the same 25-cell grid.
- `tests/e2e/x-e2e-happy-path.spec.ts` — the two cases.

## Acceptance criteria → test cases

- **Given** a seeded Event (dense pool) and the shared link, **when** a Player joins, Marks a winning line, and opens Ranks, **then** BINGO fires and the Player ranks correctly — with no coordination beyond the link. → `join -> Mark -> BINGO -> Leaderboard completes with only the shared link (zero admin action)`: joins, taps the 4 non-free Squares of the middle row (index 12, the free centre, already counts — the AC's "centre free space counts"), asserts the `BINGO!` celebration, opens Ranks, and asserts the sole Player's row shows rank 1 with `1 bingo` / `4 squares`. The test never navigates to `/admin` or calls anything in `src/components/Admin.tsx`.
- **Given** a Player offline, **when** they Mark a Square and reload, **then** the Mark persists and later syncs on reconnect (ADR 0006). → `a Mark made offline survives a reload and syncs on reconnect (ADR 0006)`: `context.setOffline(true)`, Marks an arbitrary unmarked non-free Square, asserts it renders `marked` immediately (latency compensation), then reloads while STILL offline. The reload wipes memory and re-initializes the whole app from scratch, so anything that survives came from the durable IndexedDB queue — the SW (real build, below) serves the shell + JS offline, so the Firestore SDK re-initializes and recovers the pending Mark. (See "App gap" below: the app's UI cannot cold-boot fully offline, but the SDK-level queue recovery — the ADR 0006 property — is independent of the UI having rendered.) It then reconnects and polls an independent rules-disabled Firestore read of THIS Player's own board — `boards/{uid}`, the uid captured from the page's Firebase Auth IndexedDB persistence right after join, never the reloaded tab's own cache and never an any-board scan (which could false-pass on a prompt-text collision with the happy-path Player's board in the same shared Event) — until the emulator shows the Mark: since the Mark was made offline and never synced before the reload, its only path to the emulator is the durable queue surviving the reload and draining on reconnect — the rigorous "not memory" proof, matching `tests/offline/w0-offline-persistence.test.ts`'s ordering discipline. Finally a fresh ONLINE reload asserts the app renders the Mark, so the durable Mark reaches the UI end-to-end too (a fresh reload, never "wait for the offline-booted page to recover" — see "App gap" below).
- **App gap observed while building the offline case (documented, deliberately asserted *around*, not pinned): the app cannot cold-boot offline, and its post-reconnect recovery is nondeterministic.** After the offline reload, `App.tsx` holds on "Loading…": `AuthContext`'s `onAuthStateChanged` callback `await`s `ensureUserProfile` — a Firestore *transaction*, which requires the network — plus (since #23/#112) the `readAdultAttestation` bootstrap, before it publishes `loading: false`. Offline, those reads fail/retry and the signed-in shell never renders, despite the service worker having served the shell and the persistent cache holding the last-seen Board. And once that bootstrap has *failed* offline, recovery after reconnect is nondeterministic (observed flaking under Playwright): the failed bootstrap does not reliably re-run on its own. That belongs to a follow-up app ticket (offline cold-boot: publish the cached auth User without awaiting network-bound bootstraps, retry them in the background). This suite therefore proves durability via the emulator observer and the UI via a fresh online reload, so the e2e pins ADR 0006 without also pinning the current bootstrap behavior.
- Dense pool ≥ 24 active Prompts (ADR 0004's `dealBoard` guard, `src/game/logic.ts` `MIN_POOL`). → asserted directly against the seeded `ITEMS` length in `beforeAll`.
- Zero-coordination (shared-link-only) path. → structural: neither case performs any admin action or reads `Admin.tsx`; `join.ts` is the only entry point either case uses.

## Emulator wiring (delivered)

The originally-anticipated blocker (`@playwright/test` / the e2e harness itself) never applied: `package.json` already carries `@playwright/test` and `test:e2e` (`w0-test-harness` landed it). The one real gap was that `src/firebase.ts` exported its `auth`/`db`/`storage` singletons wired to production Firebase only — no file under `src/**` called `connectAuthEmulator` / `connectFirestoreEmulator` / `connectStorageEmulator`, so the browser-served app could not reach the Local Emulator Suite this suite seeds. Every existing emulator-connected test (`tests/offline/**`, `tests/rules/**`) sidesteps this by constructing its OWN separate Firebase app instance directly in Node; none makes the real browser-served app talk to the emulator, and no in-scope test-only workaround could (Playwright cannot drive a real Google OAuth popup, and `firebase/auth` ships no `__FIREBASE_DEFAULTS__`/env shim that would redirect the singleton to the emulator from outside `src/`). An independent verifier confirmed the two cases failed at exactly `joinViaSharedLink`'s final assertion for this reason and nowhere else.

That branch is now delivered in `src/firebase.ts`, gated on `import.meta.env.MODE === 'e2e' && import.meta.env.VITE_FIREBASE_PROJECT_ID?.startsWith('demo-')`. It calls `connectAuthEmulator`/`connectFirestoreEmulator`/`connectStorageEmulator` against the ports `firebase.json` pins (auth 9099, firestore 8080, storage 9199). The `demo-` project-id check is belt-and-suspenders (the same emulator-only convention `tests/offline` and `tests/rules` use); `MODE === 'e2e'` is the load-bearing gate.

**Why `MODE === 'e2e'` and not `import.meta.env.DEV`.** The offline case reloads the page *while offline*, which only resolves when a precaching service worker serves the app shell — and vite-plugin-pwa emits that SW only for a build, never for `vite dev`. So `playwright.config.ts`'s app webServer serves a real `vite build --mode e2e` + `vite preview`, where `import.meta.env.DEV` is `false`. `MODE` is a built-in Vite env var (unlike an unset `VITE_*` flag, which Vite does not statically replace, so it would NOT dead-code-eliminate), statically substituted at build time: `--mode e2e` bakes in `'e2e'`, so the branch is kept and runs; the real production build (`npm run build`, `MODE === 'production'`) folds the gate to `'production' === 'e2e'` → `false` and drops the whole branch. `join.ts` then drives the Auth Emulator's account-chooser popup that `signInWithPopup` opens (add a new auto-generated account, submit — no real Google OAuth; a brief retry absorbs the widget's handler-attachment delay), and both cases pass. The suite runs serially (`workers: 1`) because both cases join the same seeded Event and the happy-path case asserts it is the sole leaderboard Player.

**Production is unaffected.** Because `MODE === 'production'` in `npm run build`, `'production' === 'e2e'` folds to `false` and `false && …` dead-code-eliminates the whole branch — the shipped `dist/` bundle carries no `connectAuthEmulator`/`connectFirestoreEmulator`/`connectStorageEmulator` call and no emulator host string (`127.0.0.1:9099`/`:8080`/`:9199`), verified by grepping the built bundle after `npm run build`. `src/firebase.test.ts` (the ADR 0006 source guard) stays green: under `npm test` (Vitest, `MODE === 'test'`) `'test' === 'e2e'` is `false`, so the branch never runs.

`npm run test:e2e` (Playwright, real Chromium) remains a **local-only** layer — `.github/workflows/app-ci.yml` says so explicitly ("intentionally not run here") — so it does not run in `app-ci`; `tests/e2e/smoke.spec.ts` (`w0-test-harness`) still proves the runner launches Chromium independently.
